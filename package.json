{
  "name": "ok-buddy-cli",
  "version": "0.1.1",
  "description": "Returns a random new post from user input subreddit and converts it to ascii.",
  "main": "buddy.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "https://github.com/dolanske",
  "license": "MIT",
  "dependencies": {
    "canvas": "^2.9.0",
    "chalk": "^5.0.0"
  },
  "bin": {
    "buddy": "./buddy.js"
  }
}
